export default function Stats() {

    // will probably use a statsReducer used for deleting data, loading data for a period of time, sorting data etc

    return (
        <>
            <h1>Your Stats</h1>
        </>
    )

}
